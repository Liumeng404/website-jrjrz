# 2026-07-09 工作日志

## 完成：Astro + Decap CMS 博客项目初始化（步骤 1-8）
按 `blog-astro-decap-implementation-plan.md` 执行：
- Astro 7 minimal + strict TS 初始化。坑：`create-astro` 在非空目录（有 plan 文件）会建到子目录 `virtual-visual/`，需 `shopt -s dotglob && mv virtual-visual/* . && rmdir virtual-visual` 移上来。
- `src/content.config.ts`：blog 集合 schema（title/description/pubDate/updatedDate/draft），用 `glob` loader + `astro/zod`。
- 布局/页面：BaseLayout / BlogPost / index / about / blog/index（列表）/ blog/[...slug]（详情，getStaticPaths + render）。
- `src/styles/global.css`：简洁中文博客风格。
- Decap CMS：`public/admin/index.html`（CDN `decap-cms@^3.0.0`）+ `config.yml`（含 TODO 占位待替换仓库名/域名）。
- Cloudflare Pages Functions：`functions/api/auth.js`（发起 GitHub 授权）+ `callback.js`（换 token + postMessage 回传 Decap）。
- `public/_redirects`（/admin/* → /admin/index.html 200）+ `public/_headers`（安全头）。
- `astro.config.mjs`：output static + site 占位 `https://example.com`。
- `.gitignore` 已含 dist/.astro/node_modules（astro 模板自带，符合要求）。
- `npm install` 成功（192 包，0 漏洞；cleanup 警告是 safe-delete 沙箱拦截，不影响安装）。
- `npm run build` 成功：生成 4 页（/, /about, /blog, /blog/hello-world）。
- `git init -b main` + 首次提交。git 身份用占位 "Blog Author <blog@example.com>"（用户后续需改）。

## 未完成（需用户操作）
- 步骤 4：旧 HTML 迁移（首页/关于页现为占位，等用户提供源码）。
- 步骤 9：GitHub OAuth App 创建（浏览器操作）。
- 步骤 10：Cloudflare Pages 连接 + 环境变量（NODE_VERSION=22、GITHUB_CLIENT_ID、GITHUB_CLIENT_SECRET 选 Secret 类型）。
- `config.yml` 占位符替换（repo + base_url）。
- `astro.config.mjs` site 替换。
- git 身份改回用户自己的。

## 环境/工具坑
- dev server 启动被沙箱 safe-delete 拦截（vite 清 .vite/deps 超 50 文件阈值），需 `dangerouslyDisableSandbox: true` 重启。

## 旧站视觉迁移（步骤 4 补完）
- 用户提供 4 个旧 HTML（首页/chatgpt/Claude/supergrok 子页），是 Chrome 源码查看器格式（table 切片+转义），用 python 脚本提取还原干净 HTML 后分析。
- 旧站「露梦AI Store」是 Apple 风格：`#0066cc` 主色 / `#1d1d1f` ink / `#f5f5f7` parchment / 18px 圆角卡片 / 药丸按钮(9999px) / 毛玻璃二级导航 / 渐变文字(primary→dream-b→dream-c) / system-ui 17px/1.47/-0.374px / 容器 min(100%,1180px)。
- 旧站本身是纵向 section 布局，无侧边栏；用户要求沿用视觉但博客改「左正文+右侧边栏」。
- 重写 `src/styles/global.css`：完整还原设计 token + 新增两栏 `.layout`（grid minmax(0,1fr) + 300px，<900px 单栏，aside sticky top:68px）+ prose + widget 卡片。
- `BaseLayout.astro`：top-nav(黑 44px sticky) + logo(渐变圆形 mark+文字) + footer(3 列 grid)。新增 `src/components/Sidebar.astro`（关于/最近文章/导航 widget）。
- 首页 hero(parchment)+两栏；列表/详情/关于都用 `.layout` 两栏；`BlogPost.astro` 改为纯内容片段。站名「露梦的博客」。
- `npm run build` 通过。旧 4 个 HTML 参考文件仍在根目录，待用户决定归档/删除。

## 重大纠偏：整站从「纯博客」改回「营销展示站」
用户指出 jrjrz.com 本质不是纯博客，而是 **AI 会员代充值营销站**（露梦AI Store：ChatGPT Plus / Claude / SuperGrok 代充，引导加微信），博客只是为 SEO 加的内容栏目。之前错把整站做成纯博客。
- 旧站首页 body 确认是营销结构：hero(代充值) + service-card 产品卡(3 AI 平台) + 信任条 + 开通流程 steps + FAQ + CTA banner。导航旧站：服务/流程/FAQ/联系客服。
- `BaseLayout.astro`：品牌「露梦的博客」→「露梦AI Store」；默认 title/description 去掉"记录 AI、技术与思考"；导航改为 首页 / 服务(/#services) / 博客(/blog) / 关于(/about)；footer 文案改营销口吻。
- `src/styles/global.css`：新增营销首页样式（hero-brands / cards-grid / service-card / card-icon / card-price / card-btn / trust-strip / steps / faq / cta-banner + 营销首页响应式）。复用已有 token。
- `src/pages/index.astro`：整个重写为营销首页（hero + 3 服务卡 + 信任条 + 三步流程 + FAQ 折叠 + CTA 联系客服）。FAQ 折叠用 `<script>` 切换 .active。
- `src/pages/about.astro`：从博客作者风改为营销站「关于我们」页（坚持原账号开通/流程透明/便捷支付 + 联系）。仍为两栏布局 + Sidebar。
- `src/pages/blog/index.astro` 与 `blog/[...slug].astro`：title 统一品牌「露梦AI Store」。
- `npm run build` 通过（4 页）。dev server 4321 活着可预览。
- 注意：价格处暂写「价格私询」（避免编具体金额误导），用户上线前替换为真实价格；客服微信二维码旧站有但用户未提供素材，CTA 暂用占位链接。

## 回滚：撤销营销站改造，回到轮1（纯博客+Apple风格）
- 用户选择「回滚上个版本」，撤销最后一轮营销站改造（轮2），恢复到轮1状态（纯博客+旧站Apple风格+两栏，品牌「露梦的博客」）。
- 还原文件：BaseLayout（品牌/导航/footer 回轮1）、index（博客列表hero+两栏）、about（博客作者风）、blog/index 与 blog/[...slug] 的 title 回轮1。global.css 与 BlogPost 轮2未动，保持轮1。
- 提交轮1为 1 个 commit `523f698`（8 文件，+834/-250）。仓库现 2 commit：init + 此。
- 未跟踪的 `.workbuddy/` 与 4 个旧 HTML 参考文件(+`_files/`) 仍不进 git（正确，不提交）。
- 教训：把整站做成营销站前应先确认。用户要的基础是「纯博客+Apple风格」这版，营销元素后续再融入（不全站覆盖）。另：轮2我漏了给 global.css 加营销样式（index 引用了未定义的 .service-card 等类），导致营销首页无样式——回滚时反倒无需处理 global.css。
