{
  "name": "virtual-visual",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=lumengwebsite",
    "baidu:push": "node scripts/baidu-push.mjs"
  },

  "dependencies": {
    "astro": "^7.0.7"
  }
}